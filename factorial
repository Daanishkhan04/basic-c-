#include <stdio.h>

int main()
{
  int i,n;
  int fact=1;
  printf("***WELCOME***\n");
  printf("Enter no = ");
  scanf("%d",&n);
  
  for(i=1;i<=n;i++)
  {
      fact=i*fact;
  }
  {
      printf("factorial is %d ",fact);
  }
}
